# ⚠️ Руководство по безопасному использованию парсера Compass

## ⚠️ ВАЖНО: Ограничения и риски

**НЕТ, запускать парсер без ограничений НЕ рекомендуется!** Это может привести к:

1. **Блокировке IP адреса** - Compass.com может заблокировать ваш IP при слишком частых запросах
2. **Нарушению Terms of Service** - Автоматический парсинг может нарушать условия использования сайта
3. **Юридическим проблемам** - В некоторых юрисдикциях массовый парсинг без разрешения может быть незаконным
4. **Перегрузке серверов** - Слишком много запросов могут навредить инфраструктуре сайта

## ✅ Рекомендации по безопасному использованию

### 1. Используйте настройки по умолчанию

Парсер уже настроен с безопасными параметрами:
- `concurrency=5` - максимум 5 одновременных запросов
- `request_delay=0.5` - задержка 0.5 секунды между запросами
- Автоматическая обработка rate limiting (429 ошибок)

### 2. Начните с малых объемов

```python
# Начните с небольшого количества объявлений
python run_full_parse.py new-york 50
```

### 3. Добавьте дополнительные задержки для больших объемов

Если нужно парсить много данных, увеличьте задержки:

```python
parser = CompassParser(
    client,
    concurrency=3,  # Еще меньше одновременных запросов
    request_delay=1.0,  # 1 секунда между запросами
    respect_rate_limit=True
)
```

### 4. Мониторьте ошибки

Следите за логами на наличие:
- `429 Too Many Requests` - слишком много запросов
- `403 Forbidden` - возможная блокировка
- Частые таймауты

### 5. Делайте перерывы

Для больших объемов данных:
- Делайте перерывы между сессиями парсинга
- Разбивайте на несколько запусков
- Используйте разные временные интервалы

### 6. Используйте разумные лимиты

```bash
# Хорошо: парсить по 100-200 объявлений за раз
python run_full_parse.py new-york 100

# Плохо: пытаться парсить тысячи объявлений сразу
python run_full_parse.py new-york 10000
```

## 🛡️ Встроенные механизмы защиты

Парсер уже включает:

1. **Rate Limiting Detection** - автоматически обрабатывает 429 ошибки
2. **Exponential Backoff** - увеличивает задержки при ошибках
3. **Request Delays** - задержки между запросами
4. **Concurrency Limits** - ограничение одновременных запросов
5. **Retry Logic** - повторные попытки при временных ошибках

## 📊 Рекомендуемые настройки

### Для небольших объемов (< 100 объявлений)
```python
parser = CompassParser(
    client,
    concurrency=5,
    request_delay=0.5
)
```

### Для средних объемов (100-500 объявлений)
```python
parser = CompassParser(
    client,
    concurrency=3,
    request_delay=1.0
)
```

### Для больших объемов (500+ объявлений)
```python
parser = CompassParser(
    client,
    concurrency=2,
    request_delay=2.0
)
```

## ⚖️ Юридические аспекты

1. **Проверьте Terms of Service** Compass.com перед использованием
2. **Используйте данные ответственно** - только для личных/исследовательских целей
3. **Не перепродавайте данные** без разрешения
4. **Соблюдайте авторские права** на контент

## 🔍 Мониторинг

Следите за:
- Количеством успешных запросов
- Частотой ошибок 429
- Временем выполнения
- Размером полученных данных

Если видите много ошибок 429 - **увеличьте задержки и уменьшите concurrency**.

## 💡 Советы

1. **Тестируйте на малых данных** перед большими парсингами
2. **Сохраняйте промежуточные результаты** - парсинг может прерваться
3. **Используйте VPN** если нужна дополнительная защита (но это не гарантирует отсутствие блокировок)
4. **Читайте логи** - они покажут, что происходит

## 🚨 Что делать при блокировке?

Если получили блокировку:
1. **Немедленно остановите парсер**
2. **Подождите несколько часов/дней**
3. **Уменьшите агрессивность** (больше задержек, меньше concurrency)
4. **Используйте другой IP** если необходимо продолжить

## ✅ Итог

**Используйте парсер ответственно:**
- ✅ Начинайте с малых объемов
- ✅ Используйте рекомендуемые настройки
- ✅ Мониторьте ошибки
- ✅ Делайте перерывы
- ✅ Соблюдайте Terms of Service

**НЕ:**
- ❌ Запускайте без ограничений
- ❌ Парсите тысячи объявлений за раз
- ❌ Игнорируйте ошибки 429
- ❌ Используйте данные коммерчески без разрешения

