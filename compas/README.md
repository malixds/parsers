# Compass.com Parser

Парсер для извлечения данных об объявлениях недвижимости с сайта [compass.com](https://www.compass.com/).

## Описание

Парсер работает в три этапа:
1. **Индексация** - получение списка URL объявлений через POST API поиска
2. **Получение HTML** - загрузка HTML страниц для каждого объявления
3. **Парсинг** - извлечение данных из `window.__INITIAL_DATA__` (JSON данные на странице)

## Установка

```bash
pip install -r requirements.txt
```

## Использование

### Базовое использование

```python
import asyncio
import httpx
from compass import CompassParser

async def main():
    async with httpx.AsyncClient(timeout=30.0) as client:
        parser = CompassParser(client, concurrency=10)
        results = await parser.run(location="new-york", max_results=100)
        print(f"Обработано {len(results)} объявлений")

asyncio.run(main())
```

### Полный парсинг с сохранением в JSON

```bash
python run_full_parse.py [location] [max_results]
```

Примеры:
```bash
# Парсинг объявлений в Нью-Йорке (максимум 100)
python run_full_parse.py new-york 100

# Парсинг объявлений в Лос-Анджелесе (максимум 500)
python run_full_parse.py los-angeles 500
```

## Параметры

### CompassParser

- `client` - httpx.AsyncClient для HTTP запросов
- `source_name` - имя источника (по умолчанию "compass")
- `concurrency` - количество одновременных запросов (по умолчанию 10)
- `save_html_every` - сохранять каждый N-й HTML файл (по умолчанию 20)
- `html_save_dir` - папка для сохранения HTML (по умолчанию "htmls")

### Метод run()

- `location` - локация для поиска (по умолчанию "new-york")
- `max_results` - максимальное количество результатов (по умолчанию 1000)

## Структура данных

Парсер извлекает следующие поля:

- **Обязательные:**
  - `address` - адрес недвижимости
  - `listing_id` - ID объявления
  - `listing_link` - ссылка на объявление

- **Основные:**
  - `sale_price` / `lease_price` - цена продажи/аренды
  - `listing_type` - тип объявления (sale/lease)
  - `listing_status` - статус (available/sold/pending и т.д.)
  - `size` - площадь в квадратных футах
  - `property_description` - описание недвижимости

- **Дополнительные:**
  - `photos` - список ссылок на фото
  - `agents` - список агентов
  - `listing_details` - дополнительные детали (bedrooms, bathrooms, year_built и т.д.)

## Особенности

1. **Извлечение данных из JSON**: Парсер использует `window.__INITIAL_DATA__` на странице, что позволяет получать структурированные данные без сложного парсинга HTML.

2. **POST API для поиска**: Используется POST запрос к `/api/search` для получения списка объявлений с пагинацией.

3. **Обработка ошибок**: Автоматические ретраи при сетевых ошибках с экспоненциальной задержкой.

4. **Сохранение HTML**: Опциональное сохранение HTML файлов для отладки (каждый N-й файл).

## Формат выходных данных

Результаты сохраняются в JSON файл с именем `parsed_results_YYYYMMDD_HHMMSS.json`.

Каждое объявление представлено объектом `DbDTO` со всеми извлеченными полями.

## ⚠️ ВАЖНО: Безопасность и ограничения

**НЕ запускайте парсер без ограничений!** Это может привести к блокировке IP и нарушению Terms of Service.

Парсер уже настроен с безопасными параметрами по умолчанию:
- `concurrency=5` - ограничение одновременных запросов
- `request_delay=0.5` - задержка между запросами
- Автоматическая обработка rate limiting (429 ошибок)

**📖 Подробное руководство:** См. [SAFETY_GUIDE.md](SAFETY_GUIDE.md) для рекомендаций по безопасному использованию.

## Примечания

- Compass.com может иметь защиту от автоматического парсинга. Рекомендуется использовать разумные значения `concurrency` и добавлять задержки между запросами при необходимости.

- Структура `window.__INITIAL_DATA__` может изменяться, поэтому парсер использует несколько путей для извлечения данных.

- Для разных локаций может потребоваться настройка параметров поиска в методе `get_listing_urls_from_search()`.

- **Начинайте с малых объемов** (50-100 объявлений) для тестирования перед большими парсингами.

