# 🏢 Парсер недвижимости rwholmes.com

Асинхронный парсер для сбора данных о коммерческой недвижимости с сайта [rwholmes.com](https://rwholmes.com). Парсер извлекает информацию об объявлениях, валидирует данные через Pydantic DTO и сохраняет результаты в JSON формате.

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Архитектура](#-архитектура)
- [Установка](#-установка)
- [Быстрый старт](#-быстрый-старт)
- [Детальное описание](#-детальное-описание)
- [Структура данных](#-структура-данных)
- [Примеры использования](#-примеры-использования)
- [Статистика](#-статистика)

---

## ✨ Возможности

### Основные функции:
- ✅ **Асинхронный парсинг** - высокая скорость обработки (до 10 одновременных запросов)
- ✅ **Автоматическая индексация** - получение всех URL из sitemap
- ✅ **Валидация данных** - все данные проходят через Pydantic DTO схемы
- ✅ **Извлечение полных данных** - адреса, цены, описания, фото, MLS номера и многое другое
- ✅ **Обработка ошибок** - автоматическое следование редиректам и обработка исключений
- ✅ **Детальное логирование** - отслеживание процесса парсинга

### Извлекаемые данные:
- 📍 Адрес недвижимости
- 💰 Цены (продажа/аренда)
- 📏 Размеры (площадь в квадратных футах)
- 📝 Описания объявлений
- 📸 Фотографии (до 31 фото на объявление)
- 🏷️ MLS номера
- 📄 Brochure PDF файлы
- 📊 Статусы объявлений (available, leased, sold)
- 🏢 Типы объявлений (sale/lease)
- И многое другое...

---

## 🏗️ Архитектура

### Структура проекта

```
parsers/
├── rwholmes.py      # Основной парсер
├── schema.py              # Pydantic DTO схемы
├── run_full_parse.py      # Скрипт для полного парсинга
├── requirements.txt       # Зависимости
└── README.md             # Документация
```

### Класс RwholmesParser

Парсер реализован как класс `RwholmesParser` с тремя основными этапами:

#### 1️⃣ Этап 1: Индексация
- Получает sitemap: `https://rwholmes.com/estate_property-sitemap.xml`
- Парсит XML и извлекает все URL объявлений
- Фильтрует только URL вида `/properties/` или `/property/`

#### 2️⃣ Этап 2: Получение HTML
- Асинхронно загружает HTML для каждого объявления
- Использует семафор для контроля конкурентности
- Автоматически следует редиректам (301/302)
- Обрабатывает ошибки сети

#### 3️⃣ Этап 3: Парсинг данных
- Парсит HTML через BeautifulSoup
- Извлекает данные через специализированные методы
- Создает DbDTO объекты с валидацией
- Возвращает список валидированных объектов

### Методы извлечения данных

```python
extract_address()         # Адрес недвижимости
extract_price()           # Цена и тип (sale/lease)
extract_size()            # Размер площади
extract_description()     # Описание объявления
extract_listing_status()  # Статус (available, sold, leased)
extract_details()         # Детали из таблиц
extract_photos()          # Ссылки на фотографии
extract_brochure_pdf()    # Ссылка на PDF брошюру
extract_mls()             # MLS номер
extract_listing_id_from_url()  # ID из URL
```

---

## 📦 Установка

### Требования
- Python 3.8+
- pip

### Установка зависимостей

```bash
pip install -r requirements.txt
```

**Зависимости:**
- `httpx` - асинхронные HTTP запросы
- `beautifulsoup4` - парсинг HTML
- `lxml` - быстрый XML/HTML парсер
- `pydantic[email]` - валидация данных через DTO
- `fake-useragent` - случайные User-Agent заголовки

---

## 🚀 Быстрый старт

### Полный парсинг всех объявлений

```bash
python3 run_full_parse.py
```

Этот скрипт:
- ✅ Автоматически получает все URL из sitemap
- ✅ Парсит все объявления (до 10 одновременно)
- ✅ Сохраняет результаты в JSON файл с временной меткой
- ✅ Показывает подробную статистику

**Результат:** Файл `parsed_results_YYYYMMDD_HHMMSS.json`

### Прямой запуск парсера

```bash
python3 rwholmes.py
```

### Использование как модуль

```python
import asyncio
import httpx
from rwholmes import RwholmesParser

async def main():
    async with httpx.AsyncClient() as client:
        parser = RwholmesParser(
            client=client,
            source_name="rwholmes",
            concurrency=10  # Количество одновременных запросов
        )
        results = await parser.run()
        return results

# Запуск
results = asyncio.run(main())
```

---

## 📚 Детальное описание

### Инициализация парсера

```python
parser = RwholmesParser(
    client: httpx.AsyncClient,      # HTTP клиент
    source_name: str = "rwholmes",  # Имя источника
    concurrency: int = 10            # Количество одновременных запросов
)
```

### Основные методы

#### `get_listing_urls() -> list[str]`
Получает все URL объявлений из sitemap.

```python
urls = await parser.get_listing_urls()
# Возвращает: ['https://rwholmes.com/properties/...', ...]
```

#### `parse_listing(url: str) -> DbDTO | None`
Парсит одно объявление и возвращает DbDTO объект.

```python
dto = await parser.parse_listing('https://rwholmes.com/properties/...')
if dto:
    print(dto.address)
    print(dto.price)
```

#### `run() -> list[DbDTO]`
Основной метод - запускает полный цикл парсинга.

```python
results = await parser.run()
# Возвращает список DbDTO объектов
```

### Конфигурация

Парсер настраивается через параметры конструктора:

- **concurrency** - количество одновременных запросов (по умолчанию 10)
  - Больше значение = быстрее, но больше нагрузка на сервер
  - Рекомендуется: 5-15

- **source_name** - имя источника данных (по умолчанию "rwholmes")
  - Используется в поле `source_name` всех записей

---

## 📊 Структура данных

### DbDTO (Data Transfer Object)

Все данные валидируются через Pydantic модель `DbDTO` из `schema.py`.

#### Обязательные поля:
```python
source_name: str        # "rwholmes"
listing_id: str         # ID объявления
listing_link: str       # Полный URL
address: str            # Адрес недвижимости
```

#### Основные поля:
```python
listing_type: Optional[str]      # "sale" | "lease" | None
listing_status: Optional[str]    # "available" | "sold" | "leased" | ...
sale_price: Optional[str]        # "$1,875,000"
lease_price: Optional[str]       # "$17.50"
size: Optional[str]              # "9,092 SF"
property_description: Optional[str]  # Полное описание
mls_number: Optional[str]        # MLS номер
photos: Optional[List[str]]      # Список URL фотографий
brochure_pdf: Optional[str]      # URL PDF брошюры
listing_details: Optional[Dict]  # Дополнительные детали
```

#### Географические поля:
```python
city: Optional[str]
state: Optional[str]
zipcode: Optional[str]
coordinates: Optional[str]
```

#### Полный список полей:
См. `schema.py` для полного списка всех доступных полей.

### Пример DbDTO объекта

```json
{
  "source_name": "rwholmes",
  "listing_id": "70-blanchard-road-burlington",
  "listing_link": "https://rwholmes.com/properties/70-blanchard-road-burlington/",
  "listing_type": "lease",
  "listing_status": "available",
  "address": "70 Blanchard Road, Burlington",
  "sale_price": null,
  "lease_price": "$15.00",
  "size": "2,876 SF",
  "property_description": "Modern office space...",
  "mls_number": "GheEhUAgqgUUAFRySIgAh",
  "photos": [
    "https://rwholmes.com/wp-content/uploads/.../photo1.jpg",
    ...
  ],
  "brochure_pdf": "https://rwholmes.com/wp-content/uploads/.../brochure.pdf"
}
```

---

## 💻 Примеры использования

### Пример 1: Парсинг одного объявления

```python
import asyncio
import httpx
from rwholmes import RwholmesParser

async def parse_one():
    async with httpx.AsyncClient() as client:
        parser = RwholmesParser(client, concurrency=1)
        url = "https://rwholmes.com/properties/70-blanchard-road-burlington/"
        result = await parser.parse_listing(url)
        
        if result:
            print(f"Address: {result.address}")
            print(f"Price: {result.lease_price or result.sale_price}")
            print(f"Photos: {len(result.photos) if result.photos else 0}")
        else:
            print("Failed to parse")

asyncio.run(parse_one())
```

### Пример 2: Парсинг с ограничением количества

```python
async def parse_limited():
    async with httpx.AsyncClient() as client:
        parser = RwholmesParser(client, concurrency=5)
        
        # Получаем все URL
        urls = await parser.get_listing_urls()
        
        # Парсим только первые 10
        results = []
        for url in urls[:10]:
            dto = await parser.parse_listing(url)
            if dto:
                results.append(dto)
        
        return results
```

### Пример 3: Сохранение результатов

```python
import json
from rwholmes import RwholmesParser

async def save_results():
    async with httpx.AsyncClient() as client:
        parser = RwholmesParser(client)
        results = await parser.run()
        
        # Сохраняем в JSON
        with open('results.json', 'w', encoding='utf-8') as f:
            json.dump(
                [r.model_dump() for r in results],
                f,
                indent=2,
                ensure_ascii=False,
                default=str
            )
        
        print(f"Saved {len(results)} listings")
```

### Пример 4: Фильтрация результатов

```python
async def filter_listings():
    async with httpx.AsyncClient() as client:
        parser = RwholmesParser(client)
        results = await parser.run()
        
        # Только объявления на продажу с ценой
        for_sale = [
            r for r in results 
            if r.listing_type == 'sale' and r.sale_price
        ]
        
        # Только доступные объявления
        available = [
            r for r in results 
            if r.listing_status == 'available'
        ]
        
        print(f"For sale: {len(for_sale)}")
        print(f"Available: {len(available)}")
```

---

## 📈 Статистика

### Реальные результаты парсинга

**Последний запуск:** 189 объявлений

#### Заполненность полей:

| Поле | Заполнено | Процент |
|------|-----------|---------|
| Обязательные поля | 189/189 | 100% ✅ |
| Описание | 189/189 | 100% ✅ |
| Фото | 189/189 | 100% ✅ |
| MLS номер | 189/189 | 100% ✅ |
| Размер | 185/189 | 97.9% ✅ |
| Brochure PDF | 113/189 | 59.8% |
| Цена (любая) | 58/189 | 30.7% |

#### Распределение типов:
- **For Lease:** 39 объявлений (20.6%)
- **For Sale:** 19 объявлений (10.1%)
- **Не указано:** 131 объявление (69.3%)

#### Распределение статусов:
- **Available:** 160 объявлений (84.7%)
- **Leased:** 25 объявлений (13.2%)
- **Sold:** 4 объявления (2.1%)

#### Статистика по фото:
- **Всего фото:** 1,105 изображений
- **Среднее на объявление:** 5.8 фото
- **Мин/Макс:** 2-31 фото

#### Производительность:
- **Скорость:** ~5.5 объявлений/сек
- **Время парсинга 189 объявлений:** ~35 секунд

---

## 🔧 Настройка и оптимизация

### Управление конкурентностью

```python
# Медленнее, но меньше нагрузка
parser = RwholmesParser(client, concurrency=5)

# Быстрее, но больше нагрузка
parser = RwholmesParser(client, concurrency=15)
```

### Таймауты

Таймауты настраиваются в методе `get_html()`:
```python
timeout=10.0  # секунд
```

### Логирование

```python
import logging

# Настройка уровня логирования
logging.basicConfig(level=logging.INFO)  # или DEBUG для подробностей
```

---

## ⚠️ Обработка ошибок

Парсер автоматически обрабатывает:

- ✅ **HTTP ошибки** - логируются и пропускаются
- ✅ **Редиректы** - автоматическое следование (301/302)
- ✅ **Таймауты** - повтор не выполняется, ошибка логируется
- ✅ **Некорректные данные** - валидация через Pydantic

### Пример обработки ошибок:

```python
results = await parser.run()

# Ошибки уже обработаны внутри run()
# Результаты содержат только успешно распарсенные объявления
print(f"Successfully parsed: {len(results)}")
```

---

## 📝 Логирование

Парсер использует стандартный Python logging:

```
2025-11-29 03:19:48 - INFO - [1] Получаю sitemap: https://rwholmes.com/...
2025-11-29 03:19:51 - INFO - [1] Найдено 197 ссылок на объявления
2025-11-29 03:19:51 - INFO - [2-3] Начинаю обработку 197 объявлений...
2025-11-29 03:19:52 - INFO - ✓ [1/197] listing-1
2025-11-29 03:19:52 - INFO - ✓ [2/197] listing-2
...
```

---

## 🐛 Отладка

### Включение подробного логирования

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Проверка одного объявления

```python
# Тестирование парсинга конкретного URL
url = "https://rwholmes.com/properties/..."
result = await parser.parse_listing(url)

if result:
    print(result.model_dump_json(indent=2))
else:
    print("Failed to parse")
```

### Сохранение HTML для анализа

```python
html = await parser.get_html(url)
with open('debug.html', 'w') as f:
    f.write(html)
```

---

## 📄 Формат результатов

Результаты сохраняются в JSON формате:

```json
[
  {
    "source_name": "rwholmes",
    "listing_id": "...",
    "listing_link": "...",
    "address": "...",
    ...
  },
  ...
]
```

### Использование результатов

```python
import json

# Загрузка
with open('parsed_results_20251129_032336.json', 'r') as f:
    listings = json.load(f)

# Обработка
for listing in listings:
    print(f"{listing['address']} - {listing.get('listing_type', 'N/A')}")
```

---

## 🔄 Обновление данных

Для обновления данных просто запустите парсер снова:

```bash
python3 run_full_parse.py
```

Новый файл будет создан с новой временной меткой. Старые файлы сохраняются для истории.

---

## 📚 Дополнительные ресурсы

- **Схема данных:** `schema.py` - полное описание DbDTO модели
- **Статистика:** `STATISTICS_REPORT.md` - детальный анализ результатов
- **Исходный код:** `rwholmes.py` - реализация парсера

---

## 🎯 Заключение

Парсер rwholmes.com - это полнофункциональное решение для сбора данных о коммерческой недвижимости. Он обеспечивает:

- ✅ Высокую скорость парсинга (асинхронность)
- ✅ Надежность (обработка ошибок, валидация)
- ✅ Полноту данных (извлечение всех доступных полей)
- ✅ Удобство использования (простой API)

**Готов к использованию в production!**

---

**Версия:** 1.0  
**Последнее обновление:** 2025-11-29  
**Автор:** Parsers Team

