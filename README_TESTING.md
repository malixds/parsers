# 🧪 Инструкция по тестированию парсера rwholmes.com

## Установка зависимостей

```bash
pip3 install -r requirements.txt
```

## Быстрое тестирование

### 1. Тест одного объявления (рекомендуется для начала)

```bash
python3 test_quick.py
```

Этот скрипт:
- ✅ Парсит одно тестовое объявление
- ✅ Проверяет валидацию DTO
- ✅ Сохраняет результат в `test_single_result.json`
- ✅ Показывает все извлеченные поля

### 2. Интерактивное тестирование с меню

```bash
python3 test_parser.py
```

Меню:
1. Тест одного объявления (быстро)
2. Тест извлечения URL из sitemap
3. Тест парсинга 3 объявлений
4. Полный запуск (первые 10 объявлений)
5. Полный запуск (все объявления)
6. Запустить все тесты по порядку

### 3. Полный запуск парсера (рекомендуется)

```bash
python3 run_full_parse.py
```

**Этот скрипт:**
- ✅ Парсит все объявления из sitemap
- ✅ Показывает подробную статистику
- ✅ Сохраняет результаты в JSON файл с временной меткой
- ✅ Показывает скорость парсинга и заполненность полей

**Результат:** Файл `parsed_results_YYYYMMDD_HHMMSS.json` со всеми данными

### 4. Альтернативный способ (простой)

```bash
python3 members_moodys.py
```

Простой запуск без статистики, выводит результат в консоль.

## Что проверяется

✅ **Валидация DTO** - все данные проходят через Pydantic валидацию  
✅ **Обязательные поля** - `source_name`, `listing_id`, `listing_link`, `address`  
✅ **Извлечение данных** - адрес, цена, размер, описание, фото, MLS номер  
✅ **Обработка ошибок** - корректная обработка редиректов и ошибок

## Результаты тестирования

После успешного запуска вы увидите:
- Количество обработанных объявлений
- Статистику по заполненным полям
- JSON файл с результатами

## Пример вывода

```
✅ УСПЕШНО! Объявление распарсено

📍 Address: 70 Blanchard Road, Burlington
🆔 Listing ID: 70-blanchard-road-burlington
📏 Size: 2,876 SF
📊 Status: sold
📸 Photos: 14
```

## Возможные проблемы

### Ошибка: `email-validator is not installed`
```bash
pip3 install 'pydantic[email]'
```

### Ошибка: `ModuleNotFoundError`
```bash
pip3 install -r requirements.txt
```

### Ошибка с редиректами
Клиент автоматически следует редиректам (301/302).

